# Gen Alpha legacy redirects

The Gen Alpha directory now exists only to redirect legacy URLs to the Slang app.
If you touch these files, keep the HTML lean with the existing two-space indentation
and make sure the refresh scripts continue to send visitors to `../slang/`.

No datasets live here anymore, so you don't need to run the slang maintenance scripts
after editing these stubs.
